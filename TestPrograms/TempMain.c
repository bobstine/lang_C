/*			Test Code for Density Estimation			8 Apr 88 ... Continued testing.	6 Apr 88 ... Created to test density estimation routines.	*/#include <stdio.h>#include "Globals.h"#include "ArrayDef.h"#include "InOut.h"#include "DensityEstimation.h"#include "PlotWindow.h"#ifdef PROTO	int main(void);	int printf (...);#endif	#define	NCOLS	2#define NOBS	10#define NROWS	32main(){	MATRIX density;	WindowPtr theWindow;	int i;	AllocMatrix (NROWS, 2, &density);	density.pntr[0][0] = 0.0; density.pntr[1][0] = 0.0;	for (i=1; i<NROWS; ++i)	{	density.pntr[0][i] = .25 + density.pntr[0][i-1];		density.pntr[1][i] = (double)(i);		/* only needed for early printing */	}	printf ("Ready to plot window\n");	pwCreatePlotWindow (&density, &theWindow);	MoveWindow (theWindow, 200,200, 1);}