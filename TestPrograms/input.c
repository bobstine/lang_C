#include "Globals.h"#include <stdio.h>#include "ArrayDef.h"#include "InOut.h"main(argc, argv)int argc;			/* count includes tool name */char *argv[];{	int nReps, arg, arOrder;	extended realValue;	VECTOR vec;		AllocVector (1, &vec);	fprintf (stderr, "First argument treated as nReps, rest are coefs...\n");	if (argc <= 2)		fprintf (stderr, "Need to supply nReps and coefs of generating process.\n");	else	{	for (arg=0; arg<argc; ++arg)		{	fprintf (stderr, "argv[%d] --->%s<---\n", arg, argv[arg]);		}		sscanf (argv[1], "%d", &nReps);		arOrder = 0;		for (arg=2; arg<argc; ++arg) 		{	sscanf (argv[arg], "%nf", &vec.pntr[0]);			++arOrder;		}		fprintf (stderr, "integer = %d with floating real[%d] = %f\n", nReps, arOrder, vec.pntr[0]);	}	PrintVector (&vec, "vec echo");	DisposeVector (&vec);}