/*		-------   Fourier  Transforms  --------------								26 Mar 88 ... Interface code separated from implementation.	*/		# include "Globals.h"#ifdef PROTO#include "ArrayDef.h"	void ftTransform (int sign, MATRIX *input);		/* In place fouier transform */			void ftTransformPair (int sign, MATRIX *input, MATRIX *out1, MATRIX *out2);		/* Input is overwritten;  first col is one series, second is other real series. */			void ftConvolve (MATRIX *input, MATRIX *ftData, MATRIX *ftKernel);		/* Input is overwritten;  two col array with one as the kernel and second as data. */#else	void ftTransform ();	void ftTransformPair ();	void ftConvolve ();#endif