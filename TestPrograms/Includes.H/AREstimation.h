/*				AREstimation.h					*//*									18 Oct 87	*//*								rev	25 Oct 87	*/#include "ArrayDef.h"#include "ARDef.h"void UseMeanCorrectionInAR (int pick);void UseFixedSpaceInAR (int P, int d);	/* Forces one time internal allocation of data space, as in simulation. */void FreeFixedSpaceInAR (void);	/* Dispose internally allocated, before call to get new dims.	*/	void EstimateARModel (MATRIX *data, ARMODEL *ywEsts, ARMODEL *lsEsts);	/*	Uses order from the size of coef portion of MODEL.	*/					