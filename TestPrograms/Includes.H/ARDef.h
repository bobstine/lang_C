/*				ARDef.h						*//*								17 Oct 87	*//*							rev				*/#include "ArrayDef.h"#ifndef ARDEF#define ARDEFenum ARType {scalar, multivar};typedef struct {				enum ARType type;				union {					VECTOR sCoef;					CUBE   mCoef;				} coef;	}	ARMODEL;void  AllocARModel (int arOrder, int dim, ARMODEL *model);	/* Allocs internal space, inits first to 1, and zeros rest. */void  PrintARModel (ARMODEL *model, char *str);#endif