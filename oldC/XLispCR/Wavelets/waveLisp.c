LVAL xswavelet(V){  LVAL x, coefs, result = NIL;    if (seqp(peekarg(0)) and seqp(peekarg(1))) {    x = xlgetarg();    coefs = xlgetarg();    result = wavelet(x, indices);  }  else if (darrayp(peekarg(0))) {    xlsave1(indices);    x = xlgetarg();    indices = makeargvec(xlargc, xlargv);    result = subarray(x, indices, FALSE, NIL);    xlpop();  }  else xlbadtype(xlgetarg());  return(result);}