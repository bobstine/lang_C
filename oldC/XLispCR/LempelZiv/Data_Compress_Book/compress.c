#include <stdlib.h>#include <stdio.h>#include <time.h>#include "arith.h"#include "errhand.h"//----------------------------------------------------------------------void main (void);//----------------------------------------------------------------------void main(void){    BIT_FILE *output;    FILE *input;    int bitCount;    input = fopen("test.input", "rb" );    if ( input == NULL )        fatal_error( "Error opening %s for input\n", "test.input" );    output = OpenOutputBitFile( "test.out" );    if ( output == NULL )        fatal_error( "Error opening %s for output\n", "test.out" );    printf( "\nCompressing %s to %s\n", "test.input", "test.out" );    CompressFile(input, output, &bitCount);    printf("Wrote %d bits\n", bitCount);    CloseOutputBitFile( output );    fclose( input );}