/*			InOut.h								*//*								 7 Oct 87		*//*						rev		19 Mar 88		*//*		Note:									*//*			(1) All system IO via this module.	*/#ifndef _InOut_#define _InOut_#include "ArrayDef.h"void Wait (void);			/* Pauses until keystroke */	void PromptInt(char *str, int *i);void PromptStr (char *str, char *inputStr);int ErrorSum (void);void SetErrorSum(int value);	/* Read and set internal error code. */	void PostErrorMessage (int code, char *message);	/* Adds code to internal error sum and posts message. */void SetPrintFormat (int fieldWidth, int nDec);	/* Controls # decimals used in output of reals. */void PrintVector (Vector *vec, char *str);void PrintVec (double *vec, int len, char *str);     /* Prints out first len of vector with the title string. */void PrintMatrix (Matrix *mat, char *str);void PrintMat (double *mat[], int nRows, int nCols, char *str);    /* List the elements of the matrix with a string for identification.*/    /* Assumes mat holds pointers to pointers to cols of the array		*/void PrintCube (Cube *theCube, char *str);#endif