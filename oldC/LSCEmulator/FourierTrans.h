/*		-------   Fourier  Transforms  --------------								26 Mar 88 ... Interface code separated from implementation.	*/		#include "ArrayDef.h"	void ftTransform (int sign, Matrix *input);		/* In place Fourier transform.		*/			void ftTransformPair (int sign, Matrix *input, Matrix *out1, Matrix *out2);		/* Input is overwritten;  first col is one series,		   second is other real series.		*/			void ftConvolve (Matrix *input, Matrix *ftData, Matrix *ftKernel);		/* Input is overwritten;  two col array with one		       as the kernel and second as data.		*/