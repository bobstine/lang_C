/*				ARDef.h						*//*								17 Oct 87	*//*							rev				*/#ifndef _ARDEF_#define _ARDEF_#include "ArrayDef.h"enum ARType {scalar, multivar};typedef struct {				enum ARType type;				union {					Vector sCoef;					Cube   mCoef;				} coef;	}	ARModel;void  arCreateScalarModel (Matrix *coefs, ARModel *model);	/* Allocates and places coefs into model structure. */void arCreateVectorModel (Matrix *coefs, ARModel *model);	/* Dimension of model set by number of rows, order is #cols/#rows. */	 void  arAllocModel (int arOrder, int dim, ARModel *model);	/* Allocs internal space, inits first to 1, and zeros rest. */void  arDisposeModel (ARModel *model);#endif