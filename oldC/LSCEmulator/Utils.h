/*			Utils.h								17 Jul 89 ... Improve error handling with ErrorHasOccured.	18 Jul 89 ... Add code for monitoring errors.	 7 Oct 87		*/int IsNearZero (double x);     /* Checks to see if |x| is within tolerance of zero. */int IsNegative (double x);	/* Checks if x is too close to being negative*/	double PolyValue (double *coef, int maxPower, double x);    /* Evaluates polynomial via Horner; uses coef[0] as const */ void ClearErrorCodes(void);	/* Reset to look like no errors have occurred */ void SetErrorCode(int errorCode, char *msg);	/* Sets error code with clarifying message. */int ErrorHasOccured (void);	/* Returns error code of last error without decrementing to next error. */int ErrorCode(char **itsMsg);	/* Retrieve error code information; drecrements pointer to next error. */