/*		Orthogonal Polynomial estimation header file.				( see Morrison, Section 4.2 )		10 Mar 88 ... Modify for more useful simulation structure.	 9 Mar 88 ... Created.	 	 	 NOTES:	(1) No space allocations done within this unit for 	 			external use. (ie, anything allocated is disposed)*/#include "ArrayDef.h"#ifdef PROTO void opFillMatrix (MATRIX *polyMat, VECTOR *polySS);	/* Fills polyMat with linear, quad, ..., ortho polys		constant fit in later routines without appearing here.		PolySS vector used below in estimation.  */	void opEstimate (VECTOR *data, MATRIX *polyMat, VECTOR *polySS, VECTOR *coefs);	/* Fills vector with estimated coefs; order chosen by	   the length of coefs vector.  Warning if too big for mat */	   void opCalcResids (VECTOR *data, VECTOR *coefs, MATRIX *polyMat, VECTOR *resids);	/* Fills resids vector using fitted model. */#elsevoid opFillMatrix ();void opEstimate ();void opCalcResids ();#endif