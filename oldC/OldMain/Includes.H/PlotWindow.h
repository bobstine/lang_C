/*	--------------------  Plot Windows  -----------------			14 Apr 88 ... Revised to have less redundant behavior with usual windows.	 7 Apr 88 ... Created to support the kernel estimation routines.	*/#include "Globals.h"#include "ArrayDef.h"#include "WindowMgr.h"void pwSetPlotWindowSize(int h, int v);	/* Alter the default size (200,200) of a plot window. */void pwSetPlotWindowLoc (int h, int v);	/* Alter the default location (0,0) of plot windows. */void pwCreatePlotWindow (MATRIX *data, WindowPtr *aWindow);	/* Opens window	with screen coordinates of the given rect. *//*	Note: use window manager to move, resize etc... */